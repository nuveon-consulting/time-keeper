{
  "name": "time-keeper",
  "displayName": "Time Keeper",
  "description": "Low-friction time tracking for Cursor and VS Code.",
  "version": "0.0.11",
  "publisher": "nuveon",
  "license": "UNLICENSED",
  "icon": "media/logo.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/nuveon/time-keeper.git",
    "directory": "packages/extension"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:timeKeeper.startTask",
    "onCommand:timeKeeper.stopTask",
    "onCommand:timeKeeper.switchTask",
    "onCommand:timeKeeper.resumePrevious",
    "onCommand:timeKeeper.statusBarClick",
    "onCommand:timeKeeper.openSummary",
    "onCommand:timeKeeper.buildTimesheetText",
    "onCommand:timeKeeper.setupMcp"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "timeKeeper.openSummary",
        "title": "Time Keeper: Open summary",
        "icon": "$(table)"
      },
      {
        "command": "timeKeeper.buildTimesheetText",
        "title": "Time Keeper: Build timesheet text…",
        "icon": "$(file-text)"
      },
      {
        "command": "timeKeeper.setupMcp",
        "title": "Time Keeper: Set up MCP…",
        "icon": "$(server-process)"
      },
      {
        "command": "timeKeeper.startTask",
        "title": "Time Keeper: Start…"
      },
      {
        "command": "timeKeeper.stopTask",
        "title": "Time Keeper: Stop"
      },
      {
        "command": "timeKeeper.switchTask",
        "title": "Time Keeper: Switch…"
      },
      {
        "command": "timeKeeper.resumePrevious",
        "title": "Time Keeper: Resume previous"
      },
      {
        "command": "timeKeeper.statusBarClick",
        "title": "Time Keeper: Status bar menu"
      }
    ],
    "keybindings": [
      {
        "command": "timeKeeper.startTask",
        "key": "ctrl+shift+;",
        "mac": "cmd+shift+;"
      },
      {
        "command": "timeKeeper.stopTask",
        "key": "ctrl+shift+'",
        "mac": "cmd+shift+'"
      },
      {
        "command": "timeKeeper.resumePrevious",
        "key": "ctrl+shift+,",
        "mac": "cmd+shift+,"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "test": "npm run compile",
    "package": "vsce package --no-dependencies"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.2.1",
    "typescript": "^5.6.3"
  }
}
