{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compile-time-keeper",
      "type": "shell",
      "command": "npm run compile --prefix packages/extension",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": "build",
      "problemMatcher": ["$tsc"]
    }
  ]
}
